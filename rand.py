#!/usr/bin/env python

import os

# code for the rand index calculation used to evaluate the classifiers
# this code will call the "classify" method for each group
# and display the score for both classifiers
